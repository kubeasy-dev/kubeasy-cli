---
phase: 06-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/deployer/const.go
  - internal/constants/const.go
  - internal/deployer/infrastructure.go
  - internal/deployer/infrastructure_test.go
autonomous: true
requirements:
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - "ComponentResult type exists with Name, Status, Message fields and StatusReady/StatusNotReady/StatusMissing constants"
    - "Version constants for nginx-ingress, Gateway API CRDs, cert-manager, cloud-provider-kind exist in deployer/const.go with Renovate annotations"
    - "Path constants KubeasyConfigDir, KindConfigPath, CloudProviderKindBinPath exist in constants/const.go"
    - "writeKindConfig(cfg) writes kind-config.yaml to ~/.kubeasy/kind-config.yaml"
    - "hasExtraPortMappings() reads kind-config.yaml and returns true only when 8080/8443 port mappings are present"
    - "All new unit tests pass with task test:unit"
  artifacts:
    - path: "internal/deployer/const.go"
      provides: "Version vars for 4 new components with Renovate annotations"
      contains: "NginxIngressVersion"
    - path: "internal/constants/const.go"
      provides: "Path constants for kubeasy config dir and binary"
      contains: "KubeasyConfigDir"
    - path: "internal/deployer/infrastructure.go"
      provides: "ComponentResult type + writeKindConfig + hasExtraPortMappings"
      exports:
        - ComponentResult
        - ComponentStatus
        - StatusReady
        - StatusNotReady
        - StatusMissing
  key_links:
    - from: "internal/deployer/infrastructure.go"
      to: "internal/constants/const.go"
      via: "KindConfigPath constant"
      pattern: "constants\\.KindConfigPath"
    - from: "internal/deployer/infrastructure_test.go"
      to: "internal/deployer/infrastructure.go"
      via: "ComponentResult unit tests"
      pattern: "TestComponentResult"
---

<objective>
Lay the foundation for per-component infrastructure setup: version constants, path constants, the ComponentResult type, and Kind cluster config file I/O.

Purpose: All subsequent plans (02, 03, 04) depend on ComponentResult to return per-component status. Kind config I/O (writeKindConfig + hasExtraPortMappings) is needed by setup.go (plan 04) to detect and prompt for cluster recreation.

Output: Updated const files, ComponentResult type in infrastructure.go, Kind config helper functions, unit tests.
</objective>

<execution_context>
@/Users/paul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-infrastructure-foundation/06-CONTEXT.md
@.planning/phases/06-infrastructure-foundation/06-RESEARCH.md

<interfaces>
<!-- Existing code the executor must understand -->

From internal/deployer/const.go (current state):
```go
// renovate: datasource=github-releases depName=kyverno/kyverno
var KyvernoVersion = "v1.17.1"

// renovate: datasource=github-releases depName=rancher/local-path-provisioner
var LocalPathProvisionerVersion = "v0.0.34"
```

From internal/constants/const.go (current state — no config dir constants):
```go
var KubeasyClusterName = "kubeasy"
var KubeasyClusterContext = "kind-kubeasy"
var KindNodeImage = "kindest/node:v1.35.0"
```

From internal/deployer/infrastructure.go (current state):
```go
// IsInfrastructureReadyWithClient checks infrastructure readiness using the provided client.
func IsInfrastructureReadyWithClient(ctx context.Context, clientset kubernetes.Interface) (bool, error)
```

Existing test helpers in infrastructure_test.go:
```go
func makeDeployment(namespace, name string, replicas int32, ready bool) *appsv1.Deployment
func makeNamespace(name string) *corev1.Namespace
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add version constants and path constants</name>
  <files>internal/deployer/const.go, internal/constants/const.go</files>
  <behavior>
    - NginxIngressVersion = "v1.15.0" with Renovate annotation (datasource=github-releases depName=kubernetes/ingress-nginx)
    - GatewayAPICRDsVersion = "v1.2.1" with Renovate annotation (datasource=github-releases depName=kubernetes-sigs/gateway-api)
    - CertManagerVersion = "v1.19.4" with Renovate annotation (datasource=github-releases depName=cert-manager/cert-manager)
    - CloudProviderKindVersion = "v0.10.0" with Renovate annotation (datasource=github-releases depName=kubernetes-sigs/cloud-provider-kind)
    - KubeasyConfigDir = "$HOME/.kubeasy" — resolved at runtime via os.UserHomeDir()
    - KindConfigPath = "$HOME/.kubeasy/kind-config.yaml"
    - CloudProviderKindBinPath = "$HOME/.kubeasy/bin/cloud-provider-kind"
    - Path constants use os.UserHomeDir() + filepath.Join for portability
    - Test: Each version constant embeds the version in the URL functions (tested in infrastructure_test.go once URL functions added in Task 2 — defer URL test to infrastructure_test.go)
  </behavior>
  <action>
    Add 4 new version vars to internal/deployer/const.go following the exact Renovate annotation pattern of KyvernoVersion. Each var must have the descriptive comment above it and the renovate comment on the line immediately before the var declaration.

    Add 3 path constants to internal/constants/const.go. Implement as functions (GetKubeasyConfigDir(), GetKindConfigPath(), GetCloudProviderKindBinPath()) returning os.UserHomeDir() + path — this makes them testable and avoids init() ordering issues. Use filepath.Join for path construction.

    Do NOT add these as bare var strings — home dir must be resolved at call time, not at package init.
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/kubeasy-cli && go build ./internal/deployer/... ./internal/constants/...</automated>
  </verify>
  <done>go build succeeds; NginxIngressVersion, GatewayAPICRDsVersion, CertManagerVersion, CloudProviderKindVersion exist in deployer/const.go; GetKindConfigPath() and GetCloudProviderKindBinPath() exist in constants/const.go</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ComponentResult type and Kind config I/O to infrastructure.go</name>
  <files>internal/deployer/infrastructure.go, internal/deployer/infrastructure_test.go</files>
  <behavior>
    - ComponentStatus type (string) with constants: StatusReady="ready", StatusNotReady="not-ready", StatusMissing="missing"
    - ComponentResult struct: Name string, Status ComponentStatus, Message string
    - writeKindConfig(cfg *kindv1alpha4.Cluster) error — marshals cluster config to YAML and writes to GetKindConfigPath(); creates ~/.kubeasy dir if missing
    - hasExtraPortMappings() bool — reads kind-config.yaml; returns true if node[0] has ExtraPortMappings with HostPort 8080 AND 8443; returns false if file missing or ports absent (no error returned — absence = false)
    - Test: ComponentResult with StatusReady has Status=="ready"
    - Test: ComponentResult with StatusNotReady has Status=="not-ready"
    - Test: ComponentResult with StatusMissing has Status=="missing"
    - Test: hasExtraPortMappings returns false when file does not exist
    - Test: hasExtraPortMappings returns true when config has 8080/8443 port mappings
    - Test: hasExtraPortMappings returns false when config has different ports
    - Test: writeKindConfig round-trips — write then read back hasExtraPortMappings returns true
  </behavior>
  <action>
    Add ComponentStatus, StatusReady/StatusNotReady/StatusMissing constants, and ComponentResult struct at the top of infrastructure.go (before existing functions).

    Add writeKindConfig and hasExtraPortMappings functions. writeKindConfig uses sigs.k8s.io/kind/pkg/apis/config/v1alpha4 types (already in go.mod). Marshal to YAML using sigs.k8s.io/yaml (check go.mod — if not present, use encoding/json then convert, or add sigs.k8s.io/yaml which is already a transitive dep of kind).

    hasExtraPortMappings reads and unmarshals kind-config.yaml. Check for HostPort 8080 AND 8443 in any node's ExtraPortMappings. If file absent → return false (not an error — means cluster was created without this config file).

    Write tests in infrastructure_test.go using t.TempDir() for the config file path — pass path as parameter to testable versions (writeKindConfigToPath, hasExtraPortMappingsAt) so tests don't write to the real home directory.

    Use adapter functions: writeKindConfig calls writeKindConfigToPath(cfg, GetKindConfigPath()) and hasExtraPortMappings calls hasExtraPortMappingsAt(GetKindConfigPath()) — enables testability without mocking.
  </action>
  <verify>
    <automated>cd /Users/paul/Workspace/kubeasy/kubeasy-cli && go test ./internal/deployer/ -run "TestComponentResult|TestHasExtraPortMappings|TestWriteKindConfig" -v</automated>
  </verify>
  <done>All ComponentResult and Kind config tests pass; task test:unit green</done>
</task>

</tasks>

<verification>
cd /Users/paul/Workspace/kubeasy/kubeasy-cli && task test:unit
</verification>

<success_criteria>
- ComponentResult type exported from internal/deployer with StatusReady/StatusNotReady/StatusMissing
- 4 new version constants in deployer/const.go with Renovate annotations
- Path helper functions in constants/const.go using os.UserHomeDir()
- writeKindConfigToPath and hasExtraPortMappingsAt tested with fake paths
- task test:unit passes with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-foundation/06-01-SUMMARY.md`
</output>
